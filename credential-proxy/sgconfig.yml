# ast-grep project configuration for credential-proxy.
# Run from the repo root: ast-grep scan --config credential-proxy/sgconfig.yml credential-proxy/
# Or from credential-proxy/: ast-grep scan --config sgconfig.yml .
ruleDirs:
  - rules
