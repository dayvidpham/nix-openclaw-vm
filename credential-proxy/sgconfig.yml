# ast-grep project configuration for credential-proxy.
# Run from credential-proxy/: ast-grep scan --config sgconfig.yml .
# Or individual rules from repo root: ast-grep scan -r credential-proxy/ast-grep/<rule>.yml credential-proxy/
ruleDirs:
  - ast-grep
