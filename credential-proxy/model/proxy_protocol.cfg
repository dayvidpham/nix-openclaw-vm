\* Add statements after this line.
\* TLC configuration for proxy_protocol.tla
\*
\* Run: tlc proxy_protocol.tla -config proxy_protocol.cfg

SPECIFICATION Spec

\* Safety invariants: checked in every reachable state.
INVARIANTS
    TypeInvariant
    NoDoubleDecision

\* Liveness properties: checked under WF fairness from "fair process".
PROPERTIES
    HandlerEventuallyCompletes
    RegistryEventuallyCleanedUp

\* TLC will also check for deadlock (no enabled actions) by default.
CHECK_DEADLOCK TRUE
